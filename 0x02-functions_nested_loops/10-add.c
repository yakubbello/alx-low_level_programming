#include "main.h"
/**
 * add- adds two numbers and return the result
 * @a: first argument
 * @b: second argument
 *
 * Return: always return the result of the operation
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
